<h1>Facturadores</h1>

<table>
  <tr>
    <th>Facturador</th>
  </tr>

  <% @billers.each do |biller| %>
      <tr>
        <td><%= 'Holi' %></td>
        <td><%= link_to 'Show', biller_path(biller) %></td>
      </tr>
  <% end %>
</table>