<h1>Llamadas</h1>

<table>
  <tr>
    <th>Desde</th>
    <th>Hasta</th>
  </tr>

  <% @calls.each do |call| %>
      <tr>
        <td><%= call.origin %></td>
        <td><%= call.destiny %></td>
        <td><%= link_to 'Show', call_path(location) %></td>
      </tr>
  <% end %>
</table>